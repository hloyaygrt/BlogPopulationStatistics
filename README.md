# BlogPopulationStatistics

Comparision of different solutions to problem "find the number of different numbers on array range online".

Solution5: persistent segment tree (O(log(n)) insertion, O(log(n)) per query).
